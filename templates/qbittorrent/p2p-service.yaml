{{- if .Values.services.qbittorrent.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: qbittorrent-p2p
  namespace: {{ .Values.common.namespace }}
  labels:
    app.kubernetes.io/name: qbittorrent
spec:
  type: NodePort #ClusterIP
  ports:
    - name: p2p
      port: {{ .Values.services.qbittorrent.ports.p2p }}
      targetPort: p2p
      protocol: TCP
      nodePort: {{ .Values.services.qbittorrent.nodePorts.p2p }}
    - name: p2pudp
      port: {{ .Values.services.qbittorrent.ports.p2pudp }}
      targetPort: p2pudp
      protocol: UDP
      nodePort: {{ .Values.services.qbittorrent.nodePorts.p2pudp }}
  selector:
    app.kubernetes.io/name: qbittorrent
{{- end }}
